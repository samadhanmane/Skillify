# Frontend Environment Configuration

# API configuration (Vite uses VITE_ prefix for environment variables)
VITE_API_PORT=4000
VITE_API_URL=http://localhost:4000/api

# Environment mode
NODE_ENV=development

# Production settings (uncomment when deploying)
# VITE_API_URL=/api
# NODE_ENV=production

# Backend Configuration
VITE_BACKEND_PORT=4000 